// Template — copy to `engineConfig.local.ts` (gitignored) and fill in.
// engineClient.ts imports the local variant. Per dev / per tunnel session.

export const ENGINE_URL = 'https://YOUR-NGROK-HOST.ngrok-free.app';
export const ENGINE_SHARED_SECRET = 'paste-the-value-from-engine/.env';
